name = 'Vamsi Krishna' #str
print(f'Length of name: {len(name)}')

quantities = [3, 2, 5] #list
print(f'Length of quantities: {len(quantities)}')

## a same len() function is being used to check the lengths of different ojects of 
## different data types. This is an example for Polymorphism